# version: "3.9"
services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - my

volumes:
  mongo-data:
    driver: local

networks:
  my:
    external: true
